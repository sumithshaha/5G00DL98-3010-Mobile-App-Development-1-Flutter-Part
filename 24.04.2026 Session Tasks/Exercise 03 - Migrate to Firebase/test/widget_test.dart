// Smoke test placeholder. The original starter widget_test.dart referenced
// classes from the pre-refactor main.dart which no longer exist after the
// modularization in this exercise. The real tests for the unit live in
// the portfolio_movie_db project (test/movie_test.dart and
// test/navigation_test.dart).
import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — see portfolio_movie_db for the real tests', () {
    expect(1 + 1, equals(2));
  });
}
